# Generated by gmk during demo runs.
.gmk-cache/

# Backend build output.
backend/build/

# Local copies of secrets — keys belong in ~/.gmk/, not in the tree,
# but ignore them defensively in case anyone copies one in by accident.
keys.json
keys
*.crt
*.key
*.csr
*.srl
!keys.example.json
